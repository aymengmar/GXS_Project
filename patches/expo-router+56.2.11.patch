diff --git a/node_modules/expo-router/build/fork/useLinking.native.js b/node_modules/expo-router/build/fork/useLinking.native.js
index 9f0c48a..07199a0 100644
--- a/node_modules/expo-router/build/fork/useLinking.native.js
+++ b/node_modules/expo-router/build/fork/useLinking.native.js
@@ -123,8 +123,12 @@ function useLinking(ref, { enabled = true, prefixes, filter, config, getInitialU
                     return url.then((url) => {
                         const state = getStateFromURL(url);
                         if (typeof url === 'string') {
-                            // If the link were handled, it gets cleared in NavigationContainer
-                            onUnhandledLinking((0, extractPathFromURL_1.extractExpoPathFromURL)(prefixes, url));
+                            // Defer setState past the current render/commit cycle to avoid
+                            // React 19 "update on unmounted component" warning when the
+                            // Linking.getInitialURL() promise resolves before mount commits.
+                            setTimeout(() => {
+                                onUnhandledLinking((0, extractPathFromURL_1.extractExpoPathFromURL)(prefixes, url));
+                            }, 0);
                         }
                         return state;
                     });
