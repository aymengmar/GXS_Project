# GXS Delivery — Backend

FastAPI backend for the GXS Delivery logistics management app.

## Setup

```bash
cd backend
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

## Run

```bash
uvicorn app.main:app --reload
```

## Test

Open: http://127.0.0.1:8000
